pipeline {
    agent any

    stages {
	stage('Executar playbook Ansible') {
            steps {
		ansiblePlaybook(
		    inventory:'ansibles/hosts',
		    playbook:'ansible/installweb.yml'
		)
            }
	}
    }
}
